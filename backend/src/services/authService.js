export async function registerUser(payload) {
  return { message: 'Chức năng đăng ký đang được phát triển', data: payload }
}

export async function loginUser({ email }) {
  return { message: 'Chức năng đăng nhập đang được phát triển', data: { email } }
}
